<section class="lede">
    <div class="container">
        <h1>{{ page.title }}</h1>
    </div>
</section>
  
{% if page.content != "" %}
<section class="container content {{ page.handle }}">
      {{ page.content }}
</section>
{% endif %}